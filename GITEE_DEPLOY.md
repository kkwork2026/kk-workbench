# KK工作台 部署到 Gitee Pages（永久稳定链接）

本说明帮你把工作台发布到 Gitee Pages，得到一个**地址固定不变、国内稳定访问**的网址。
工作台是纯静态网页（index.html + data/ + pwa/），非常适合 Gitee Pages。

---

## 你需要做的事（都在 Gitee 网页，全中文）

### 第 1 步：注册并实名
1. 打开 https://gitee.com ，点右上角「注册」，用**手机号**注册即可（全中文界面）。
2. 登录后，点右上角头像 →「设置」→「实名认证」，绑定手机号完成实名。
   ⚠️ Gitee Pages 必须实名后才能开启，这一步不能省。

### 第 2 步：新建仓库
1. 登录后点右上角「+」→「新建仓库」。
2. 仓库名称建议填 `kk-workbench`（它会决定你的网址，见第 4 步）。
3. 设为「公开」，其他默认，点「创建」。

### 第 3 步：生成私人令牌（让我帮你推送用）
1. 点右上角头像 →「设置」→ 左侧「私人令牌」→「生成新令牌」。
2. 令牌描述随便填（如 kk-workbench），**权限勾选 `projects`**（仓库读写）。
3. 点「提交」，页面会显示一串令牌字符 —— **复制下来发给我**（只显示一次）。
4. 同时把你的仓库地址（形如 `https://gitee.com/你的用户名/kk-workbench.git`）也发我。

> 把上面两样发我后，**剩下的推送、部署、开机全由我来做**，你不用碰命令行。

### 第 4 步：开启 Gitee Pages（我也可以代你用 API 开，但你自己开也行）
1. 进入仓库 → 顶部「服务」→「Gitee Pages」。
2. 部署目录选「根目录」，点击「启动」。
3. 一两分钟后得到网址：
   `https://你的用户名.gitee.io/kk-workbench/`
   这个网址**永久固定**，以后怎么更新内容都不会变。

---

## 如果你不想给我令牌，想自己传文件
进入仓库，把下面这些**原样上传到仓库根目录**（保持文件夹结构）：
- `index.html`
- `data/`（整个文件夹，含 food.js / news.js / jobs.js / fitness.js / fishing.js / budget.js / plan.js）
- `pwa/`（整个文件夹，含 manifest.json / kk-icon-192.png / kk-icon-512.png / apple-touch-icon.png）

上传完再去第 4 步开 Pages 即可。

---

## 以后怎么更新内容
- **自动更新**（每日新闻/美食/招聘/运动）：上线后我会把定时任务改成「改完数据 → 推送到 Gitee → 重新部署 Pages」，你无需操作。
- **手动改了内容后**：回到仓库的 Gitee Pages 页面点「更新」重新部署，1 分钟生效。
- **手机上添加的资料**（打卡/安排/纪念日/记账）：存在你手机本地（IndexedDB），不会因网址变化丢失；换设备才需要「💾 数据备份 → 导出/导入」。

---

## 注意事项
- Gitee Pages **免费版每次更新需手动点「更新」重新部署**（或用令牌调 API 自动部署）。
- 网址中的 `kk-workbench` 就是第 2 步的仓库名，想换短一点可以起短名（如 `kk`）。
